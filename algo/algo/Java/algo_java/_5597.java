import java.io.*;
import java.util.*;
import java.util.stream.IntStream;

public class _5597 {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        Set<Integer> s_li = new HashSet<>(new ArrayList<>(Arrays.asList(IntStream.rangeClosed(1,30).boxed().toArray(Integer[]::new))));

    }
}
