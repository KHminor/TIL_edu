# 1. pip install faker
# 아래 명령어는 (1) 무엇을 위한 명령인지 (2) 실행은 어디에서 해야하는지 작성 하시오.

#(1)시제품인 프로토타입을 개발하거나, 단위 테스트를 작성할 때 가짜 데이터가 필요할 때가 있는데 
#   이럴 때, 직접 가짜 데이터를 하드코딩 하기보단 좀 더 쉽고 빠르게 가짜 데이터를 얻을 수 있는 방법인 
#   Fake 라는 라이브러리를 사용하는 방법을 설치하는 명령어 이다.

# (2)실행은 터미널에서 실행하면 된다.

# from faker import Faker # 1 Faker 모듈의 Faker 함수를 사용하기 위한 코드이다.
# fake = Faker() # 2 Faker는 Faker라는 클래스를 통해 fake라는 객체를 만들겠다는 것이고 , fake는 객체이다.
# fake.name() # 3 name()은 fake의 이름을 불러오겠다는 것이다.


